# Database
# For local development, use a local Postgres instance or Docker
# For production on Vercel, use Vercel Postgres connection string
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/fountain_vitality?schema=public"

# NextAuth
# Generate a secret: openssl rand -base64 32
AUTH_SECRET="your-auth-secret-here-generate-with-openssl"
# Set to your production URL in Vercel
NEXTAUTH_URL="http://localhost:3000"

# Optional: Email provider for magic links
# EMAIL_SERVER_HOST=smtp.example.com
# EMAIL_SERVER_PORT=587
# EMAIL_SERVER_USER=your-email@example.com
# EMAIL_SERVER_PASSWORD=your-email-password
# EMAIL_FROM=noreply@fountainvitality.com

